import { JarvisCommandDeck } from "../apex/JarvisCommandDeck"

/** HUD home: APEX Command Deck wired to the real Jarvis task/chat engine. */
export function HudChatHome() {
  return <JarvisCommandDeck />
}

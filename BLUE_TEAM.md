# Blue team (pointer)

The home-LAN SIEM / sniffer-response spec now lives in [`SECURITY_AGENTS.md`](SECURITY_AGENTS.md) (Blue, Purple, Red-gated).

Blue (detect, alert, contain, evidence on the user’s own LAN) remains the household default. This filename is kept as a pointer so the approved name is not dropped.

Red / offensive / counter-response capability: **hard guardrail** in that file — only Taco (manual) or PolitieGPT (named LE bot, under the LE gate) may add it. Ordinary executing bots must not.

# RFC-NNNN: Short title

**Status:** draft | accepted | implemented | superseded  
**Queue item:** (optional) link to `JARVIS_MASTER_PLAN.md` Development Queue checkbox, e.g. `P1 — Playwright reliability`  
**Author:** name or session  
**Date:** YYYY-MM-DD

## Problem

What is broken, missing, or unclear? One paragraph. No architecture dump.

## Decision

What we will do (and, briefly, what we will **not** do in this RFC).

## Acceptance criteria

- [ ] Criterion 1 — testable, specific
- [ ] Criterion 2
- [ ] Unit tests pass (`python3 -m pytest`)
- [ ] (if portal touched) `npm --prefix frontend run build`

## Likely files

| Area | Paths |
| --- | --- |
| Backend | `backend/app/...` |
| Frontend | `frontend/src/...` |
| Tests | `tests/test_....py` |
| Docs | `docs/...`, queue lines in `JARVIS_MASTER_PLAN.md` §57–58 only |

## Out of scope

Explicitly list follow-ups that belong in a **separate** RFC (swarm, Browser Use, model-stack changes, etc.).

## Notes

Links, prior PRs, desktop-only verification steps.

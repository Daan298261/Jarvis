# Minimal dependencies needed to launch the reviewed HexStrike server on Windows.
# Upstream MCP and exploitation-specific Python packages are deliberately excluded.
flask>=3.1.3,<4.0.0
requests>=2.31.0,<3.0.0
psutil>=6.1.0,<8.0.0
beautifulsoup4>=4.12.0,<5.0.0
aiohttp>=3.8.0,<4.0.0
